# Round 3 — Finora

1. [ ] Accent palette switcher (not only violet) — AccentColor enum, Theme param, DataStore, Settings UI
2. [ ] AddTransaction categories → separate full-screen picker + create custom category + more defaults
3. [ ] Soften red/green everywhere (income/expense + palette + defaults)
4. [ ] Goal contribution debits a chosen account (not "from air")

Constraints: no DB schema change (adjust account.initialBalance for goal funding). No JVM locally — author carefully, brace-check.
